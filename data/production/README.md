# Reviewed production data

This directory intentionally contains no records. Add the seven JSON files listed in
`packages/curated-data/src/index.ts` only after every claim has been researched and
reviewed. The production validator and synchronizer reject the synthetic `data/`
fixtures and require the marker in this directory.
