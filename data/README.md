# Curated data

This directory contains repository-reviewed JSON records. Phase 1 includes synthetic examples only; none describe real founders, projects, wallets, funding, or wealth.

Run `pnpm validate:data` after every change. Invalid records, broken references, duplicate IDs, and missing claim-level sources fail validation and the repository build.
