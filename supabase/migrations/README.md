# Migrations

Versioned SQL migrations begin when the database schema is implemented. Every table must ship with explicit row-level-security policies; anonymous access is select-only through approved public views.
